//#include "stdio.h"

int main(void){
    char nome[20];
    //gets(); //dependendo do compilador nem pega por ser perigoso
    //no gcc (SUSE Linux) 14.3.0 não pega
    //error: implicit declaration of function ‘gets’; did you mean ‘fgets’?
}
